services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: farmsure
      POSTGRES_PASSWORD: farmsure
      POSTGRES_DB: farmsure
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
